# NEVER commit plaintext client data or the source workbook
data.json
*.xlsx
*.xlsm
.dashpass

# Duo: credentials, pulled user data, and client-name mapping stay local
.duo.env
duo.json
duo_map.json

.DS_Store
Icon?
